function setup() {
    createCanvas(windowWidth, windowHeight);
    
    strokeWeight(10);
    stroke(100); // 色番号 0~255
    fill(240);
    // circle(x座標, y座標, 直径)
    circle(width / 2, height / 2, 200);
}